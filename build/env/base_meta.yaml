# This file originally created by conda-build 2.0.1, template from
# e:\share\conda-project\conda-packages\arcgis-api, last modified Mon Sep 19 14:51:09 2016
# ------------------------------------------------
# This file is also not a strict meta.yaml file: it contains {tag} items
# That are programatically altered by ../build.py and written to 
# geosaurus/build/arcgis to be used by any 'conda build' commands
# See the ../build.py script for more details

package:
    name: arcgis
    version: {geosaurus_version}
source:
    {noarch_option}
    path: ../../src
build:
    number: '1'
requirements:
    build:
        {requirements_build} 
    run:
        {requirements_run}
test:
    imports:
        - arcgis.gis
about:
    home: https://developers.arcgis.com/python
    license: Esri Master License Agreement (MLA)
    summary: ArcGIS API for Python
