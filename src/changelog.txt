For official release notes on the API, see https://developers.arcgis.com/python/guide/release-notes/

v1.3.0
    - Initial release on PyPI (Previous releases only released through conda).
      Source code is the same as 1.3.0 on conda

v1.3.0.post1
    - A bug in setup.py that made the --no-deps flag not work. Restructured
      it using better practices. Since the actual 'arcgis' source code package
      did not change at all, just appending a .post1 to the release

v1.3.0.post2
    - Updated 'classifiers', 'long_description', etc. in setup.py. Since this
      only affects information on the generated PyPI page, and the source code
      did not change at all, appending .post2 to the release
    - Added try: except for activating jupyter widget so whole install wont
      fail
